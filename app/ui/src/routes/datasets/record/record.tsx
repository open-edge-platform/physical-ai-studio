import { View, ActionButton, Text, Flex, Key, Well, Tabs, TabList, TabPanels, Item } from '@geti/ui'

export const Record = () => {
    return (
        <View>
            Recording view
        </View>
    );
}