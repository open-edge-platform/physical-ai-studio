name: Task
description: Describe a concrete piece of work.
type: Task
body:
  - type: textarea
    id: work
    attributes:
      label: What needs to be done?
      value: |
        ## Outcome

        ## Done when
    validations:
      required: true

  - type: textarea
    id: dependencies
    attributes:
      label: Dependencies or implementation notes
      description: Link related issues or record useful technical context.
    validations:
      required: false
